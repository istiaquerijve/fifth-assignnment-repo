1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?

     getElementById = Selects a single HTML element by it id.
     getElementsByClassName = Selects all elements with a specific class.
     querySelector = Selects the first element that matches a CSS selector.
     querySelectorAll = Selects all elements that match a CSS selector.


2.How do you create and insert a new element into the DOM?

      const anElement = document.createElement("tag-name");


3.What is Event Bubbling and how does it work?

      document.getElementById("tag-name").addEventListener("click", function (){

      })

4.What is Event Delegation in JavaScript? Why is it useful?      
   
       document.getElementById("tag-name").addEventListener("click", function (){

      })
            
       Why is it useful?
       1. Better Performance
       2. Dynamic Content
       3. Cleaner Code



5.What is the difference between preventDefault() and stopPropagation() methods?

        preventDefault() = Stops the default browser behavior.
        stopPropagation() = Stops the event from bubbling to parent elements.